export default function Payroll() {
  return (
    <div style={{ padding:'40px 24px' }}>
      <div style={{ fontFamily:'var(--font-display)', fontSize:'24px', letterSpacing:'2px', color:'var(--accent)' }}>
        PAYROLL — COMING SOON
      </div>
      <p style={{ marginTop:'12px', fontSize:'13px', color:'var(--text-muted)', maxWidth:'420px', lineHeight:1.6 }}>
        Payroll integration with ADP Workforce Now is in progress. Once live, you'll be able to run payroll directly from approved timesheets. Contact{' '}
        <a href="mailto:support@postcommand.app" style={{ color:'var(--accent)', textDecoration:'none' }}>support@postcommand.app</a>
        {' '}with questions.
      </p>
    </div>
  )
}
